# Backend Production Environment
# Set these values on Render dashboard before deploying

# PostgreSQL connection string (from Render Postgres service)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# CORS Security - set to your Vercel frontend domain
CORS_ORIGINS=https://your-app.vercel.app

# Allow permissive CORS (not recommended for production)
ALLOW_CORS_ALL=false

# Debug mode (disable in production)
DEBUG=false
